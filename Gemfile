source "https://rubygems.org"

# Matches the gem set GitHub Pages builds with, so a local `bundle exec jekyll
# serve` behaves the same as production. Only needed if you want to preview the
# blog locally — GitHub Pages builds automatically on push regardless.
gem "github-pages", group: :jekyll_plugins
gem "webrick" # required for local serving on Ruby 3+
